describe('serviceGlobalConfig', function() {

  beforeEach(module('dndApp'));

  it('should ...', inject(function(serviceGlobalConfig) {

	//expect(serviceGlobalConfig.doSomething()).toEqual('something');

  }));

});